iniciae docoker..
(que no se te olvide arrancar su aplicacion)
(si tienes fallas intenta reiniciar el doker)
-> cd //dirijete al proyecto
-> docker-compose down -v //solo usar en caso de que no abra los servicios
-> docker-compose down //c<Blue>ierra el proyecto
-> docker-compose up -d --build //crea todo
-> docker-compose exec app bash //arranca doker</Blue>

ya estando adentro
(te ouedes dirijir a donde tengas tu proyecto)
-> python3 manage.py runserver 0:8000 //corre el proyecto
-> python3 manage.py startapp nombre
-> pip freeze //con esto podemos ver lo que tenemos instalado
-> pip freeze > ../requirements.txt
-> python3 manage.py makemigrations //para ver las migraciones
-> python3 manage.py migrate //para crear las migraciones
-> python3 manage.py createsuperuser //es para crear un super usuario

https://getbootstrap.com/docs/5.1/forms/layaut/
django forms //pagina de forms que maneja django
django queryset //pagina para ver los objetos que nos permitira hacer consulta
django class based views //vistas basadas en clases
django validators //para ver los validadores


35165885@uaz.edu.mx 


Git

git init //se inicializa el repositorio
git status //para ver los archivos modificados y ver que vamos o queremos subir
git add (nombre del archivo que se quiere subir) para suir un archivo en especifico
git add . // es para subir todo
git commit -m "descripcion de lo que se agregara"
git config --global user.email 35165885@uaz.edu.mx // se agrega el correo
git config --global user.name herminiogarcia

si es la primera ves buelve a cargar el commit
git commit -m "descripcion de lo que se agregara"

git branch -M main //es para subirlo a la rama principal

git remote add origin https://github.com/HerminioGarcia/Ayudas-Labsol.git //es para 
//subirlo al repositorio remoto

git push -u origin main //creamos el puch pero este es para 
//sincronisar con la carpeta del proyecto

git pull --rebase origin main //es para la actualizacion
git push origin main //es para suvir las cosas
git config --global pull.rebase true
git config --global rebase.autoStash true

para subir las cosas xdd
git status
git add .
git commit -m "login requerido en url usuario"
git push origin main
